# Example environment configuration file
# 1. Copy this file to .env
# 2. Fill in your real values
# 3. Never commit .env to the repository!

# ───────────────────────────────────────────────
# Database connection (required for Prisma)
# ───────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/task_manager?schema=public"

# Examples:
# Local PostgreSQL:     postgresql://postgres:postgres@localhost:5432/task_manager
# Neon.tech:            postgresql://user:xxxx@ep-xxx-123456.us-east-2.aws.neon.tech/task_manager?sslmode=require
# Supabase:             postgresql://postgres.[project-ref]:xxxx@db.project-ref.supabase.co:5432/postgres

# ───────────────────────────────────────────────
# Secret key for signing JWT tokens (change this!)
# ───────────────────────────────────────────────
JWT_SECRET=super-long-random-secret-at-least-32-characters-change-me

# How to generate a strong secret:
#   Linux / macOS:    openssl rand -base64 48
#   Node.js one-liner: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"

# ───────────────────────────────────────────────
# Application port (optional, Next.js defaults to 3000)
# ───────────────────────────────────────────────
PORT=3000

# ───────────────────────────────────────────────
# Optional variables (uncomment if needed later)
# ───────────────────────────────────────────────
# NODE_ENV=development
